// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.busko.routemanager.model.transit.gtfs;

import java.util.List;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;
import org.busko.routemanager.model.transit.gtfs.Calendar;
import org.springframework.transaction.annotation.Transactional;

privileged aspect Calendar_Roo_Jpa_ActiveRecord {
    
    @PersistenceContext
    transient EntityManager Calendar.entityManager;
    
    public static final EntityManager Calendar.entityManager() {
        EntityManager em = new Calendar().entityManager;
        if (em == null) throw new IllegalStateException("Entity manager has not been injected (is the Spring Aspects JAR configured as an AJC/AJDT aspects library?)");
        return em;
    }
    
    public static long Calendar.countCalendars() {
        return entityManager().createQuery("SELECT COUNT(o) FROM Calendar o", Long.class).getSingleResult();
    }
    
    public static List<Calendar> Calendar.findAllCalendars() {
        return entityManager().createQuery("SELECT o FROM Calendar o", Calendar.class).getResultList();
    }
    
    public static Calendar Calendar.findCalendar(Long id) {
        if (id == null) return null;
        return entityManager().find(Calendar.class, id);
    }
    
    public static List<Calendar> Calendar.findCalendarEntries(int firstResult, int maxResults) {
        return entityManager().createQuery("SELECT o FROM Calendar o", Calendar.class).setFirstResult(firstResult).setMaxResults(maxResults).getResultList();
    }
    
    @Transactional
    public void Calendar.persist() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.persist(this);
    }
    
    @Transactional
    public void Calendar.remove() {
        if (this.entityManager == null) this.entityManager = entityManager();
        if (this.entityManager.contains(this)) {
            this.entityManager.remove(this);
        } else {
            Calendar attached = Calendar.findCalendar(this.id);
            this.entityManager.remove(attached);
        }
    }
    
    @Transactional
    public void Calendar.flush() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.flush();
    }
    
    @Transactional
    public void Calendar.clear() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.clear();
    }
    
    @Transactional
    public Calendar Calendar.merge() {
        if (this.entityManager == null) this.entityManager = entityManager();
        Calendar merged = this.entityManager.merge(this);
        this.entityManager.flush();
        return merged;
    }
    
}
